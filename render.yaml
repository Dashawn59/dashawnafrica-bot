services:
  - type: worker
    name: dashawnafrica-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python telegram_bot_2025.py
    envVars:
      - key: BOT_TOKEN
        sync: false
    plan: free